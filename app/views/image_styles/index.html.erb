<%= render :partial => "sub_menu"%>

<h2>Listing Image Styles for Uploader</h2>

<table>
  <tr>
    <th>Key(Unique)</th>
    <th>Name</th>
    <th>Width</th>
    <th>Height</th>
    <th>Quality</th>
    <th>Crop</th>
    <th>Versions</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @image_styles.each do |image_style| %>
  <tr>
    <td><%= image_style.key %></td>
    <td><%= image_style.name %></td>
    <td><%= image_style.width %></td>
    <td><%= image_style.height %></td>
    <td><%= image_style.quality %></td>
    <td><%= image_style.crop %></td>
    <td><%= image_style.child_image_styles.map{|is| is.name}.join(",") %></td>
    <td><%= link_to 'Show', image_style %></td>
    <td><%= link_to 'Edit', edit_image_style_path(image_style) %></td>
    <td><%= link_to 'Destroy', image_style, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Image style', new_image_style_path %>
